** start of script.js **

function uniteUnique(...arrays) {
  const result = [];
  for (let arr of arrays) {
    for (let value of arr) {
      if (!result.includes(value)) {
        result.push(value);
      }
    }
  }

  return result;
}

** end of script.js **

